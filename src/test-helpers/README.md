# Test Helpers

Target home for browser-exposed smoke-test helpers and fixtures.

Current test exposure still happens inside the compatibility modules, especially
clinical and parser modules. Keep any exported test helpers free of patient
secrets and real clinical data.
