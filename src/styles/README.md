# Styles

`app.css` contains the former inline stylesheet extracted from `index.html`.

Keep visual changes separate from structural refactors whenever possible so
smoke-test failures are easier to interpret.
