# Integration

Target home for integration adapters.

Current FHIR export helpers are still in `src/clinical/10-therapy-validation.js`
to keep this refactor behavior-preserving. Move them here after adding a focused
test around `clinicalRecordToFhirBundle`.
