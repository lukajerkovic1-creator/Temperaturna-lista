# Utils

Target home for shared date, string, hashing, and id helpers.

For now these helpers remain in the compatibility modules so the runtime bundle
keeps the same closure and behavior. Move them here only with matching tests.
