# Audit

This audit tracks code-solvable P0/P1/P2 findings for the Temperaturna lista app. It does not claim clinical, legal, DPO, hospital IT, or production readiness.

| ID | Priority | Evidence | Risk | Reproduction | Plan | Status | Regression test |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AUD-P0-001 | P0 | `src/print/50-print-layout.js` allowed `printPages()` to proceed while `state.admin.enabled` was true. | A clinical printout could be produced from service/admin calibration mode without a hard stop. | Sign in as Luka/admin in the smoke client, open admin mode with `Ctrl+Alt+A`, click print. Before the fix the print workflow continued. | Add an early print guard that blocks service/admin mode and surfaces a visible error; add a Playwright regression test. | Closed in code; production audit remains open until a full P0/P1/P2 pass and hospital governance review. | `tests/github-pages.smoke.spec.js` -> `blocks printing while admin service mode is active` |
| AUD-P0-002 | P0 | `src/print/50-print-layout.js` displayed a print confirmation with `Nastavi ispis` when `state.lastTextOverflowWarnings` was non-empty. | A clinical printout could be produced even after the app detected text overflow into constrained list fields. | Create or inject an overflow warning, click print. Before the fix the workflow offered a continue path. | Convert overflow from confirmable warning to hard-stop; update overflow wording and add Playwright regression. | Closed in code; production audit remains open until full print/PDF regression coverage is complete. | `tests/github-pages.smoke.spec.js` -> `blocks printing when text overflow warnings are present` |
| AUD-P0-003 | P0 | `printPages()` relied on checklist UI but did not centrally block printing when required clinical prerequisites were missing. | A list could be printed without confirmed identifiers, encounter/admission date, diagnosis, allergy status, or therapy. | Fill only partial patient data and click print. Before the fix the workflow could continue to save/print confirmation instead of stopping. | Add a central print prerequisite guard and a Playwright regression that verifies no print occurs. | Closed in code; broader clinical validation and hospital governance remain production blockers. | `tests/github-pages.smoke.spec.js` -> `blocks printing when required clinical prerequisites are missing` |
| AUD-P0-004 | P0 | The form/model/print path did not carry a dedicated patient identifier or encounter/protocol identifier, and print pages lacked audit metadata. | Printed or exported lists could be harder to reconcile to the intended patient encounter, especially after local-only JSON workflows or multi-page prints. | Open a patient without MBO/MRN or protocol ID and print; before the fix the print path did not hard-stop on those missing identifiers and pages did not show the local app/version/sync metadata. | Add explicit MBO/MRN, encounter/protocol, room and bed fields; carry them into JSON, ClinicalRecordV1 and FHIR; hard-stop print when patient/encounter identifiers are missing; stamp print pages with metadata. | Closed in code; hospital-approved identifier policy and print-layout review remain production blockers. | `tests/github-pages.smoke.spec.js` -> `blocks printing when required clinical prerequisites are missing`, `builds ClinicalRecordV1, medication safety warnings and a basic FHIR Bundle`, `navigates preview page pairs and prints only the active pair` |
| AUD-P0-005 | P0 | The public app wired `Ctrl+Alt+A`, `Ctrl+Alt+P`, therapy speech input, parser-test helpers and FHIR clipboard actions without a production runtime boundary. | Development/admin workflows and unapproved data egress paths could be invoked in the clinical-facing build, including by bypassing hidden UI controls. | Load the app without a local QA bootstrap, press `Ctrl+Alt+A` or `Ctrl+Alt+P`, or inspect `window.TemperaturnaListaParserTests`; before the fix these paths were active or exposed. | Add a fail-closed runtime policy and two generated bundles. The production bundle must physically exclude admin mode/dashboard, calibration editor, parser capture/regression fixtures, speech recognition, FHIR clipboard and development shortcuts; production HTML must not mount their controls. Local QA requires localhost, `?qa=` and an explicit pre-bootstrap flag. | Closed in code. `tools/validate-static-app.js` enforces bundle separation; desktop/mobile production checks load `bootstrap.js`, expose no QA globals or controls, have no console warnings, and report no horizontal overflow. Clinical/DPO governance remains a production blocker. | `tests/security.spec.js` -> `fails closed for admin, parser capture, audio and FHIR clipboard in production mode`; local-QA admin/parser regressions in `tests/github-pages.smoke.spec.js` and `tests/privacy.spec.js` |
| AUD-P0-006 | P0 | The print guard verified that clinical fields were populated, but did not require an explicit final user review and did not hard-stop all critical structured medication validation findings. | Changed or incorrectly parsed identity, encounter, allergy, diagnosis or medication data could be printed after a presence-only check; a medication line without a recognizable dose or route could pass as informational. | Fill all required fields and print without reviewing them, or enter a medication without a dose/route. Before the fix no signed review state was required and missing dose/route was not critical. | Add three session-only confirmations bound to hashed signatures of identity/encounter, allergy and critical clinical data; invalidate the relevant confirmation after data changes; include critical record and medication-safety findings in the central print hard-stop. | Closed in code. Confirmations are not persisted and contain no cleartext clinical data. Clinical validation, approved role policy and hospital print-governance review remain production blockers. | `tests/github-pages.smoke.spec.js` -> `requires explicit final clinical confirmations before printing`, `invalidates a clinical confirmation after critical data changes`, `blocks printing for medication without a recognizable dose or route` |
| AUD-P0-007 | P0 | The local-only policy was enforced by `LOCAL_PATIENT_STORAGE_ONLY`, but the production bundle still contained the Firebase SDK dynamic import and legacy login/patient DOM, and QA admin calibration still wrote `appConfig` through the injected client. | A future flag regression or accidental handler reuse could re-enable online writes, while hidden legacy controls increased the exposed attack surface and contradicted the explicit offline-only policy. | Inspect `src/app/bootstrap.js` for `gstatic.com/firebasejs`, load production and inspect the Firebase login/patient DOM, or leave QA admin mode with Save. Before the fix the production artifact contained the SDK import and QA emitted an `appConfig` write. | Replace the production storage-client function with a fail-closed stub during bundle generation; block real Firebase client initialization whenever local-only policy is active; remove legacy Firebase interactive DOM before production startup; keep admin calibration local; enforce all invariants in static validation and Playwright network tests. | Closed in code and deployed boundary. Firestore deny-all rules were deployed on 2026-07-16; `firestore:delete --all-collections --force` completed with no remaining collection listed. Synthetic QA smoke clients remain in-memory test doubles only. | `tests/github-pages.smoke.spec.js` -> `loads the local JSON-only app without browser errors`, `saves a local patient JSON without contacting Firebase or another online storage service`, `saves admin print settings locally without an online write when leaving admin mode`; `tools/validate-static-app.js` production SDK/import assertions |
| AUD-P0-008 | P0 | `printCanvasPages()` generated page metadata from the two physical canvases being printed, so an active continuation pair 3-4 was stamped as `Stranica 1/2` and `Stranica 2/2`. | A detached or scanned continuation page carried a false document-page identity, weakening reconciliation to the intended encounter and sequence. | Open continuation pair 3-4 and print. Before the fix the preview buttons said 3/4 while print metadata said 1/2 and 2/2. | Carry each preview page's actual document number and the highest generated document page count on its export canvas; render metadata from those values while retaining the physical print count separately in the smoke event. | Closed in code. Printing active pages 3-4 now stamps `Stranica 3/4` and `Stranica 4/4`; the print event still reports two physical pages. | `tests/github-pages.smoke.spec.js` -> `navigates preview page pairs and prints only the active pair` |
| AUD-P0-009 | P0 | `downloadDowntimeBackupData()` serialized `fullName`, diagnosis, therapy and all other form data directly into a cleartext `temperaturna-lista-downtime-backup-v1` JSON file whose suggested name was derived from patient identity. | A lost, copied or misdirected downtime file exposed readable PHI/PII, and a filename alone could disclose identity. | Enter synthetic identity and clinical text, click the former `Preuzmi downtime backup`, then open the downloaded JSON. Before the fix the values and identity-derived filename were readable without a secret. | Encrypt the complete backup payload with AES-GCM-256 and a PBKDF2/SHA-256 passphrase-derived key, random salt and IV; collect secrets in a password dialog, use an identity-free random filename, apply retention expiry, reject cleartext/expired/tampered files, and record only non-PHI operational audit metadata. | Closed in code. Normal user-requested local patient JSON remains unchanged; the separate downtime workflow now uses `temperaturna-lista-encrypted-downtime-backup-v2`. Passphrases and derived keys are never stored. Hospital approval of local storage locations and key-handling procedure remains a production blocker. | `tests/github-pages.smoke.spec.js` -> `encrypts downtime backup, rejects a wrong passphrase and restores with the correct passphrase`, `refuses legacy cleartext and expired downtime backup files`; `tools/validate-static-app.js` encryption assertions |
| AUD-P0-010 | P0 | Parsed values were visually marked as autofilled, but the record did not retain a field-level source excerpt, confidence, parser version, confirmation actor/time or an independent print hard-stop for stale/unconfirmed parser provenance. | A user could not reconstruct why a critical value appeared, and a broad checkbox alone could not prove that the current parsed identity, allergy, date or medication fields were reviewed. | Paste a synthetic OHBP record, inspect the generated patient JSON and change an automatically filled identity field. Before the fix there was no per-field provenance object or UI, and print validation only knew the broad final-review signature. | Add `temperaturna-lista-parser-provenance-v1`, bounded original excerpts, confidence, value hashes and parser version; bind group confirmations to actor/time; invalidate them on change; persist provenance but never its confirmation in local JSON/encrypted recovery; hard-stop print for each current unconfirmed critical parsed field. | Closed in code. Provenance is visible and automatically expanded after parse/restore. It is local-only, contains bounded source excerpts, and restored confirmation is always reset. Parser correctness and hospital-approved confirmation policy remain production blockers. | `tests/github-pages.smoke.spec.js` -> `records field-level parser provenance and blocks print after a parsed field changes`, `persists parser provenance in local JSON but never persists its confirmation`; `tools/validate-static-app.js` provenance assertions |
| AUD-P0-011 | P0 | The first build-metadata implementation used the same token for injection and the public runtime global name. `replaceAll('__TEMPERATURNA_LISTA_BUILD_SHA__', value)` changed `window.__TEMPERATURNA_LISTA_BUILD_SHA__` into invalid syntax such as `window.7c19a9bd362c`; `node --check src/app/bootstrap.js` failed and the app never left its static shell. | The complete clinical application failed to start even though the former static validation reported success, leaving controls visible but nonfunctional. | Generate the bundle with the colliding placeholder and load the page or run `node --check src/app/bootstrap.js`. Before the fix the browser never exposed the runtime build value or clinical API, and all active workflow tests timed out against the static shell. | Use dedicated build-only placeholder tokens, retain the stable runtime global, and parse both generated bundles with Node `vm.Script` as an unconditional build gate. | Closed in code. Production and QA bundles now parse successfully and expose the same deterministic build SHA; direct runtime verification and the full desktop/mobile smoke suite pass. | `tools/validate-static-app.js` JavaScript syntax/runtime-global assertions; `tests/github-pages.smoke.spec.js` -> `loads the local JSON-only app without browser errors`; full smoke 75 passed, 35 intentionally skipped, 0 failed |
| AUD-P0-012 | P0 | Production had removed Firebase patient writes but still shipped `PatientDraftStorage`, an opt-in encrypted patient auto-save that wrote the complete clinical payload to `temperaturna_lista_pacijent_sifrirani_draft_v2` in `localStorage`; legacy cleartext drafts could also be migrated. | This contradicted the hospital-approved local-file-only model and left durable patient data inside browser-managed storage, outside the explicit JSON-file workflow. | Open the former advanced data controls, enable encrypted recovery, enter a synthetic patient and inspect `localStorage`; before the fix an encrypted patient envelope persisted across reloads and the production bundle contained its serializer/decryptor. | Physically remove the browser-draft adapter and controls from the production artifact; make every production draft save a no-op; purge both current and legacy keys at startup; retain historical encrypted-draft testing only in the explicit localhost QA bundle; add storage and static-build regressions. | Closed in code. Production persistence is manual local JSON only; startup never decrypts or migrates browser drafts. DPO/IT approval of local file handling remains a production blocker. | `tests/privacy.spec.js` -> `production purges legacy and encrypted browser patient drafts without restoring them`, `patient data entry does not persist clinical cleartext locally`; `tools/validate-static-app.js` production bundle absence/purge assertions; full smoke 75 passed/35 historical Firebase skips; privacy 6/6; targeted local-JSON/draft 10/10; quality 25 passed/1 mobile-only skip; parser 4/4; clinical 3 passed/3 intentional mobile skips; `docs/evidence/aud-p0-012-production-{desktop,mobile}.png` |
| AUD-P0-013 | P0 | The local-JSON-only production build removed Firebase login UI, but print metadata and parser confirmation still derived the actor from the empty legacy auth context and fell back to `neprijavljen` / `lokalni korisnik`. | Printed pages and parsed-field confirmations were not attributable to a named operator, weakening traceability and the required user/time provenance. | Load the production build, complete clinical fields and inspect print-frame metadata. Before the fix no operator field existed and each page used `Korisnik: neprijavljen`; parser confirmation used a generic actor. | Add a required, session-only operator full-name field; never persist it to patient JSON or browser/network storage; bind review signatures and parser confirmation actor to it; invalidate confirmations when it changes; hard-stop confirmation and print when absent; assert the operator on print pages. | Closed in code. The operator is required for confirmation/print, appears on every page, is cleared on reload and is absent from patient JSON and browser storage. Hospital identity/governance policy remains a production blocker. | `tests/github-pages.smoke.spec.js` -> `blocks clinical confirmation and print without a named session operator`, active-pair print metadata assertion; `tests/privacy.spec.js` operator non-persistence assertion; `tools/validate-static-app.js` operator hard-stop assertions; `docs/evidence/aud-p0-013-synthetic-print.{pdf,png}` |
| AUD-P0-014 | P0 | The first reproducible print-evidence PDF showed that the single-line audit metadata band could extend beyond its box and clip the rightmost sync/test markers. | A printed or scanned page could lose mandatory build, sync or downtime/test provenance even though the underlying text existed in the DOM. | Generate the former `aud-p0-013-synthetic-print.pdf` with the absolute metadata band and inspect the right edge; the line ended visibly clipped. | Put metadata in the page flex flow, bound its width, allow safe wrapping, proportionally fit the full canvas below it, and fail both smoke/evidence generation on horizontal or vertical metadata overflow. | Closed in code. The regenerated two-page A4 landscape PDF preserves the complete list and wraps every mandatory metadata value inside the page box without horizontal or vertical overflow. | Active-pair print test measures metadata client/scroll dimensions on both pages; `tools/capture-print-evidence.js` rejects overflow; `docs/evidence/aud-p0-014-synthetic-print.{pdf,png}` |
| AUD-P0-015 | P0 | The encrypted downtime workflow and print recorded bounded operational events, but ordinary `Spremi JSON` and ordinary local JSON restore recorded no event at all. | Local patient-data export/import actions could not be reconstructed even at a minimal non-PHI operational level, leaving a required audit gap. | Save a synthetic patient JSON, restore it, then inspect `temperaturna_lista_operativni_audit_v1`; before the fix neither action appeared, while encrypted downtime actions did. | Record success/failure events for regular JSON export, backup export and restore only after the corresponding outcome; keep the local event schema strictly limited to event type, time, app/build, trigger and outcome; add privacy assertions proving identity and clinical text never enter storage. | Closed in code. Regular JSON export, backup export, successful restore and rejected restore now record only bounded non-PHI metadata. The mutable browser-local log is explicitly not an institutional audit trail; an approved institutional audit service remains a production blocker. | Local JSON save and parser-provenance restore smoke tests assert event presence and absence of synthetic name, identifier, diagnosis and therapy; static production assertions require all event paths |
| AUD-P0-016 | P0 | In local-JSON-only production, `saveCurrentPatientToFirebaseBeforePrint()` always returned `localOnly`, `confirmPrintWithoutFirebaseSave()` immediately returned `true`, and `savePatientData()` never recorded that the exact current version had been exported. | An unsaved or subsequently modified patient could be printed without an explicit unsaved-copy confirmation, while the status remained misleading after a successful JSON export. Repeated clicks could also start overlapping save pickers. | Enter a complete synthetic patient without pressing `Spremi JSON`, complete review and click print. Before the fix printing continued without an unsaved-local-copy dialog. Then save JSON: the status still said to save the patient and did not distinguish later edits. | Bind successful local export to the exact patient-data signature, add explicit `exported/local-json` state, invalidate it on any later change, serialize save attempts, block unnamed export, and require an explicit print confirmation unless the current signature was exported. | Closed in code. The full desktop/mobile smoke, privacy, security, parser and clinical gates pass; local JSON remains the only production patient-persistence path. | `tests/github-pages.smoke.spec.js` local save-state, unnamed/duplicate-save and local-only print confirmation scenarios; `tools/validate-static-app.js` production invariants |
| AUD-P0-017 | P0 | The central print prerequisite guard hard-stopped every print when MBO/MRN or encounter/protocol number was absent, although copied ambulatory or department records do not always contain either number. | A clinically reviewed list with a known patient name, birth year and admission date could not be printed. Removing the check silently would create a patient-selection risk. | Fill all required synthetic clinical data, leave both identifier-number fields empty, complete the three reviews and click print. Before the fix an alert blocked printing. | Keep name, birth year, admission date and clinical checks as hard stops; move only unavailable identifier numbers to a dedicated warning dialog requiring explicit continuation; mark the missing patient ID as `NEDOSTAJE` on every printed page. | Closed in code. Cancel safely stops the print; explicit continuation prints with `ID: NEDOSTAJE`. Hospital approval of the identifier-exception policy remains a production blocker. | `tests/github-pages.smoke.spec.js` -> `warns but allows printing when patient and encounter numbers are unavailable`; `docs/evidence/aud-p0-017-{desktop,mobile}-identifier-warning.png` |
| AUD-P0-018 | P0 | Hard-stop print guards prevented urgent work whenever any required field, confirmation, overflow warning or service-mode warning was present. | A user could be unable to print a needed working copy even after seeing and accepting the risk. | Leave required synthetic fields incomplete or inject an overflow warning and click print. Before this change there was no override path. | Preserve every warning, collect hard-stop findings in one accessible dialog and require an explicit `Svejedno ispiši` action; never print silently. | Closed in code by explicit user operational requirement. Earlier hard-stop findings AUD-P0-001/002/003/006/010/013 are superseded only with respect to blocking behavior; the validations and visible warnings remain active. Hospital governance review is still required. | `tests/github-pages.smoke.spec.js` -> warning-and-override print scenarios |
| AUD-P1-001 | P1 | `package.json` reported `0.0.0`, the UI used a dated `v321_login_user_profiles_2026_06_20` string and print metadata fell back to `local-build`. JSON, encrypted backup, audit and ClinicalRecord exports could not be tied to one reproducible application build. | Incident review and comparison of printouts/exports could associate records with the wrong software revision or provide no useful build identity. | Compare `package.json`, the footer, a local patient JSON, encrypted downtime envelope, ClinicalRecord metadata and print metadata. Before the fix they used different or missing identifiers. | Make `package.json` the SemVer source, inject one deterministic source hash into production/QA bundles, propagate both identifiers through UI, JSON, encrypted recovery, audit, ClinicalRecord and print, and enforce consistency in static and Playwright tests. | Closed in code as version `0.4.0`. The 12-character build SHA is deterministic for the versioned source inputs and is never used as a secret. | `tools/validate-static-app.js` package/lock/bundle consistency assertions; `tests/github-pages.smoke.spec.js` local app, JSON save, encrypted downtime, ClinicalRecord and active-pair print tests |
| AUD-P1-002 | P1 | `.github/workflows/ci.yml` treated lint and unit tests as optional and printed a success message when the corresponding package script was absent. The project therefore had no mandatory source syntax gate or true Node unit-test gate. | A malformed source fragment or regression in deterministic build identity could reach `main` while CI remained green. | Remove the `lint` or `test` script and run the former workflow steps. Before the fix both steps exited successfully without testing anything. | Add dependency-free source linting that validates standalone scripts, the ordered application fragments and JSON; extract deterministic build metadata into a unit-testable module; add mandatory Node tests and unconditional CI steps. | Closed in code. CI now always runs `npm run lint` and `npm test`; static app validation also requires the scripts and test files to exist. | `tests/unit/build-metadata.test.js` (5 tests); `tools/lint-source.js`; `.github/workflows/ci.yml` mandatory `Lint` and `Unit tests` steps |
| AUD-P1-003 | P1 | Parser regression generation existed only behind a manually operated QA panel; no deterministic fuzz/property suite ran in CI. | Harmless formatting changes or malformed bounded input could crash the parser or silently alter critical identity, date, allergy or diagnosis output without a required test failing. | Change line endings, duplicate whitespace and add harmless separators to a synthetic OHBP record, or feed bounded random text. Before the fix CI did not exercise these properties. | Expose a narrowly scoped parser API only in the localhost QA artifact; add seeded formatting mutations and bounded random-input properties on desktop/mobile; make the suite an unconditional CI step and statically forbid the helper in production. | Closed in code. The suite covers 120 semantic formatting mutations and 160 bounded random inputs per browser project (560 parser executions total) and the production bundle does not expose the API. | `tests/parser-fuzz.spec.js` -> 4 desktop/mobile tests; `tools/validate-static-app.js` production/QA boundary assertions; `.github/workflows/ci.yml` parser gate |
| AUD-P1-004 | P1 | FHIR export described itself only as compatible JSON, declared no FHIR version or profiles, generated no `Provenance`, and `validateBasicFhirBundle()` checked only Bundle/Patient/Encounter presence. | Downstream users could mistake an unprofiled payload for an integration-ready artifact, while broken references or missing export provenance passed local fixture tests. | Generate a Bundle, remove profiles/Provenance, or insert an unresolved Patient reference. Before the fix the basic validator still returned success. | Mark the workflow experimental in UI/data/docs; declare FHIR R4 4.0.1 and local experimental profiles; generate Provenance targeting every exported resource; validate identifiers, profiles, tags, references and provenance across all synthetic fixtures, including negative corruption tests. | Closed in code as a local experimental validator. It is explicitly not a substitute for the official HL7 validator or hospital/national profiles; external integration approval remains a production blocker. | `tests/clinical-fixtures.spec.js` positive validation of every synthetic fixture and negative missing-profile/Provenance/reference test; desktop/mobile ClinicalRecord/FHIR smoke test |
| AUD-P1-005 | P1 | `index.html` was 3,243,664 bytes, `00-core-ui-state.js` 3,153,342 bytes and `50-print-layout.js` 1,641,780 bytes because the same 1.19 MB icon, a form image and the medication dataset were embedded inline. The working medication array had also silently lost 439 chunks: its joined Base64 length was invalid and decoded only 2,003,911 bytes instead of the reviewed 2,332,731-byte dataset. | Huge duplicated literals made review and change isolation unreliable, slowed startup, and allowed a truncated medication database to remain hidden behind a broad smoke test oracle. | Inspect file sizes and `data:image` occurrences, then decode `EMBEDDED_THERAPY_CSV_BASE64`. Before the fix the icon appeared three times and the therapy payload no longer matched its own metadata. | Extract immutable non-patient resources into fingerprinted `assets/`; synchronously load the local therapy asset before bootstrap; restore the complete reviewed dataset; resolve form/print URLs from `document.baseURI`; add signatures, size budgets, decoded-byte checks and browser regressions. | Closed in code. `index.html` is now about 73 KB, core about 115 KB, print about 57 KB and production bootstrap about 0.92 MB. The medication status reports all 10,257 aliases on desktop/mobile; no online patient storage was introduced. Native ES-module isolation remains a later architectural improvement. | `tools/validate-static-app.js` asset/signature/load-order/no-inline checks; `tests/unit/build-metadata.test.js` binary fingerprint test; `tests/github-pages.smoke.spec.js` medication database/autocomplete and local-only print tests; `tests/performance.spec.js` desktop/mobile budgets |
| AUD-P2-001 | P2 | The local-JSON-only build changed its machine-readable network state when the browser went offline, but its visible availability copy still only said that the local app was loaded. | Users could miss a network transition because the status was not expressed clearly in text and appeared unchanged outside its color/state attribute. | Load the local-only app, switch the browser context offline and inspect the availability status. Before the fix the visible text did not explicitly say that the app was offline or remind the user to save a local JSON copy. | Render an explicit offline message, keep Firebase patient storage identified as disabled rather than unavailable, and cover the transition in desktop/mobile Playwright smoke tests. | Closed in code. The offline message now explains that work may continue locally and that patient data must be saved manually to JSON. | `tests/github-pages.smoke.spec.js` -> `shows downtime availability when the browser goes offline` |

## Open Production Blockers

- Hospital DPO/legal/IT validation is not proven by this repository audit.
- Client-side checks are not a security boundary; production authorization must be enforced by server-side rules or an approved offline-only deployment model.
- Parser clinical correctness and medication validation require clinical validation before real patient use.
- Printed output still requires hospital review of identity, encounter, downtime, and version metadata across all page modes.

## Audit Log

- 2026-07-13: Started iterative P0/P1/P2 audit. First closed code finding: service/admin mode print block (`AUD-P0-001`).
- 2026-07-13: Closed code finding AUD-P0-002: text overflow warnings are now print hard-stops, not confirmable warnings.
- 2026-07-13: Closed code finding AUD-P0-003: print now hard-stops when required clinical prerequisites are missing.
- 2026-07-13: Closed code finding AUD-P0-004: patient identifier, encounter/protocol, room/bed and print-page metadata now flow through form data, JSON, ClinicalRecordV1/FHIR and print guards.
- 2026-07-15: Closed code finding AUD-P0-005: generated production and QA bundles are separate; production physically omits admin/calibration, parser-test, speech, FHIR clipboard and shortcut implementations and does not mount their controls. Local QA requires localhost, `?qa=` and an explicit startup flag.
- 2026-07-15: Closed code finding AUD-P0-006: print requires session-only final clinical confirmations tied to hashed data signatures, invalidates confirmations after relevant changes and blocks critical structured validation findings.
- 2026-07-15: Closed code finding AUD-P2-001: the local-only availability status now states the offline condition in text, preserves the explicit Firebase-disabled state and directs the user to save a local JSON copy.
- 2026-07-16: Closed code finding AUD-P0-007: production physically excludes Firebase SDK loading and removes legacy online UI, the online client fails closed, admin calibration persists locally, Firestore deny-all rules were deployed and the all-collections deletion command found no remaining collection.
- 2026-07-16: Closed code finding AUD-P0-008: print metadata now uses actual document page identity and total generated page count, so continuation pair 3-4 is stamped 3/4 and 4/4 rather than 1/2 and 2/2.
- 2026-07-16: Closed code finding AUD-P0-009: downtime backup now uses AES-GCM with a PBKDF2/SHA-256 passphrase-derived key, identity-free filenames, expiry, strict restore validation and non-PHI local operational audit events; legacy cleartext backups are rejected.
- 2026-07-16: Closed code finding AUD-P0-010: parser output now carries field-level excerpts, confidence, parser version, value hashes and actor/time confirmation; changed or restored critical parser fields remain print-blocking until reconfirmed. Full smoke result: 75 passed, 35 intentionally skipped legacy Firebase cases, 0 failed; quality: 25 passed, 1 desktop-inapplicable mobile check skipped, 0 failed.
- 2026-07-17: Closed AUD-P0-011 and AUD-P1-001. Build metadata now uses non-colliding injection tokens, generated production/QA JavaScript is syntax-checked, version `0.4.0` and one deterministic build SHA flow through UI, JSON, encrypted backup, local operational audit, ClinicalRecordV1 and print metadata. Verification: build/typecheck/security passed; targeted desktop/mobile 10/10; quality 25 passed/1 intentional skip; clinical 2 passed/2 intentional mobile skips; full smoke 75 passed/35 intentionally skipped legacy Firebase cases/0 failed.
- 2026-07-17: Closed AUD-P1-002. CI lint and unit tests are mandatory rather than optional no-ops. Verification: source lint passed (17 standalone scripts, 7 ordered application fragments, 5 JSON files); Node unit tests 5/5; build, typecheck and security checks passed; desktop/mobile local-only browser regression 4/4 proved local JSON save made no Firebase or online-storage request.
- 2026-07-17: Closed AUD-P1-003. A deterministic parser fuzz/property gate now runs in CI and keeps its direct parser API confined to the localhost QA bundle. Verification: 120 formatting variants and 160 bounded random inputs on both desktop and mobile; 4/4 Playwright tests passed with no browser-console or network failures.
- 2026-07-17: Closed AUD-P1-004. FHIR export is explicitly experimental, declares R4 4.0.1 and local profile URNs, includes Provenance for every exported resource, and runs stronger fixture validation. Verification: clinical fixtures 3 passed/3 intentional mobile skips; desktop/mobile ClinicalRecord/FHIR smoke 2/2; lint, typecheck, build and security checks passed.
- 2026-07-17: Closed AUD-P1-005. Multi-megabyte icon, form and therapy literals moved to fingerprinted local assets; the truncated therapy payload was restored to the reviewed 2,332,731 bytes/10,257 aliases. Static asset gates, six Node unit tests, desktop/mobile autocomplete and print checks, and six performance tests pass. Production remains local-JSON-only with no Firebase patient client.
- 2026-07-17: Closed AUD-P0-012. The production build no longer contains the patient browser-draft adapter or UI, all draft writes are no-ops, and startup purges both historical patient draft keys. Verification: lint, unit, typecheck, build and security gates passed; privacy 6/6; targeted local-JSON/draft 10/10; quality 25 passed/1 mobile-only skip; parser 4/4; clinical 3 passed/3 intentional mobile skips; full smoke 75 passed/35 intentionally skipped historical Firebase scenarios; desktop/mobile screenshots show only explicit local JSON controls and no horizontal overflow.
- 2026-07-17: Closed AUD-P0-013. The production print workflow now requires an explicitly entered session operator, binds final clinical/parser confirmation to that name, invalidates confirmation on change, and stamps it on each print page without persisting it to JSON or browser/network storage.
- 2026-07-17: Closed AUD-P0-014 after reviewing a freshly rendered synthetic PDF. Mandatory audit metadata now wraps in a bounded flow band and the full list scales below it; both pages report no measured overflow. Full verification for AUD-P0-013/014: build, lint, typecheck, 6/6 unit and security gates passed; targeted print 2/2; quality 25 passed/1 intentional desktop skip; parser 4/4; clinical 3 passed/3 intentional mobile skips; full smoke 77 passed/35 intentionally skipped historical Firebase scenarios/0 failed. Evidence: `docs/evidence/aud-p0-013-synthetic-print.{pdf,png}` and `docs/evidence/aud-p0-014-synthetic-print.{pdf,png}`.
- 2026-07-18: Identified AUD-P0-015 during the next full audit: regular local JSON export/restore lacked even the existing bounded non-PHI operational event. Added outcome-specific events and privacy regressions; full verification is pending.
- 2026-07-19: Closed AUD-P0-015. Regular local JSON export, backup export, successful restore and rejected restore now create bounded operational events containing only event type, time, app/build, trigger and outcome. Verification: build, lint, typecheck, 6/6 unit and security gates passed; targeted JSON audit 6/6 across desktop/mobile; parser 4/4; clinical 3 passed/3 intentional mobile skips; quality rerun 25 passed/1 intentional desktop skip; full smoke completed with `passed` status and no failed tests. The local mutable log is not an institutional audit trail.
- 2026-07-19: Identified AUD-P0-016 during the next full audit: local-only print bypassed unsaved-copy confirmation and local JSON export did not establish a version-bound saved state. Added exact-version export state, duplicate-save serialization, unnamed-patient export block and an explicit unsaved-print confirmation; full verification is pending.
- 2026-07-23: Opened AUD-P0-017 after confirming that copied records may legitimately omit MBO/MRN and encounter/protocol numbers. These fields now produce an explicit pre-print warning instead of a hard stop; core identity and clinical checks remain blocking.
- 2026-07-23: Closed AUD-P0-016 and AUD-P0-017 after the complete local gate: build/lint/typecheck/security/unit passed; smoke 81 passed and 35 intentionally skipped historical Firebase tests; parser 4/4; quality 25 passed with one desktop-inapplicable mobile check skipped; clinical 3 passed with three intentional mobile duplicates skipped. Desktop/mobile identifier-warning screenshots show no overflow and the browser console had no warnings or errors.
